import { styled } from "stitches.config"

export const Tag = styled('a', {
  color: '$invertText',
  background: '$invertBackground',
  fontWeight: 'medium',
  fontSize: '$sm',
  textDecoration: 'none',
  cursor: 'pointer'
})
import { styled } from "stitches.config"

export const Tag = styled('a', {
  color: 'white',
  background: '$text',
  fontWeight: 'medium',
  fontSize: '$sm',
  textDecoration: 'none',
  cursor: 'pointer'
})